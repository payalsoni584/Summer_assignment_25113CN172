# include <stdio.h>
int main(){
    int secret =25,guess;
    printf("guess the number :");
    scanf("%d",&guess);
    if(guess==secret)
    printf("correct guess!");
    else if(guess,secret)
    printf("too low!");
    else 
    printf("too high");
    return 0;
}